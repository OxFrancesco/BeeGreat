const root = new URL('..', import.meta.url).pathname.replace(/\/$/, '');
const source = await Bun.file(`${root}/AGENTS.md`).text();
const start = source.indexOf('### Main commands');
if (start < 0) throw new Error('Command reference missing');
let reference = source.slice(start).split('## Aave and Polymarket integrations')[0]!.replace('The model also has tools for', 'You can also ask for').replace('Those are natural-language capabilities', 'These are natural-language requests').replace('Generic EVM tools require the new EVM service deployment.', 'Generic contract requests are awaiting deployment.').replace('These capabilities must preserve the same wallet ownership and transaction confirmation rules as explicit commands.', 'Transactions need confirmation unless you explicitly enable YOLO.');
const headings = [
  ['Main commands', 'main'],
  ['Advanced Aerodrome reads', 'reads'],
  ['Advanced Aerodrome transaction previews', 'transactions'],
  ['Natural-language requests', 'natural'],
];
let content = Bun.markdown.html(reference).replaceAll("the sender's", "your").replaceAll("sender's", "your");
content = content.replace(/<td>Send <code>([^<]+)<\/code>\.<\/td>/g, '<td><code>$1</code></td>');
content = content.replace('<td>Send <code>/wallet</code>, then use the returned address when funding the wallet on Base.</td>', '<td><code>/wallet</code></td>');
content = content.replace('<td>Send <code>/balance</code>. Use <code>/token</code> for another token.</td>', '<td><code>/balance</code></td>');
for (const [title, id] of headings) content = content.replace(`<h3>${title}</h3>`, `<h2 id="${id}">${title}</h2>`);
content = content.replace('<p>Read options:</p>', '<details class="options"><summary>Read options</summary>').replace('<h2 id="transactions">', '</details><h2 id="transactions">');
content = content.replace('<p>Transaction options and amount rules:</p>', '<details class="options"><summary>Transaction options and amount rules</summary>').replace('<h2 id="natural">', '</details><h2 id="natural">');
const html = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="description" content="Every Pecu chat command, with explanations, usage examples, and Aerodrome options."><title>Pecu commands</title>
<style>
:root{color-scheme:light;--ink:#192738;--muted:#536477;--line:#dfe5ed;--blue:#1555cf;--paper:#fff;--code:#f3f6fa}*{box-sizing:border-box}html{scroll-padding-top:100px}body{margin:0;background:var(--paper);color:var(--ink);font:16px/1.65 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input{font:inherit}a{color:var(--blue);text-underline-offset:4px}.page{max-width:1220px;margin:auto;padding:48px 32px 80px}header{margin-left:190px;margin-bottom:32px}h1{font-size:36px;letter-spacing:-1.4px;line-height:1.15;margin:0 0 20px;font-weight:650}header p{color:var(--muted);max-width:760px;margin:12px 0}.layout{display:grid;grid-template-columns:160px minmax(0,1fr);gap:30px}aside{position:sticky;top:28px;align-self:start}aside a{display:block;padding:7px 0;color:var(--muted);text-decoration:none}aside a:hover{color:var(--blue)}.search{position:relative;margin:24px 0 16px}input{width:100%;padding:12px 14px;border:1px solid #aebccb;border-radius:7px;background:white;color:var(--ink)}input:focus{outline:3px solid #d7e5ff;border-color:var(--blue)}h2{font-size:24px;letter-spacing:-.45px;line-height:1.25;margin:44px 0 20px}h2:first-child{margin-top:0}p{margin:12px 0}code{font:14px/1.6 ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere;background:var(--code);padding:2px 4px;border-radius:3px}table{width:100%;border-collapse:collapse;table-layout:fixed;margin:18px 0 24px}th{text-align:left;font-size:14px;color:var(--muted);font-weight:500;padding:0 12px 12px}th:first-child,td:first-child{width:27%;padding-left:0}th:nth-child(2){width:34%}td{padding:22px 12px;vertical-align:top;border-top:1px solid var(--line)}td:last-child{padding-right:0}td:first-child code{background:none;padding:0;font-weight:600;color:var(--blue);font-size:14px}td:nth-child(2){font-size:15px}td:nth-child(3){font-size:14px;color:var(--muted)}.example{display:block;position:relative;margin:8px 0;padding:9px 42px 9px 10px;background:var(--code);border-radius:5px}.example code{background:none;padding:0;color:var(--ink)}.copy{position:absolute;right:5px;top:6px;border:0;background:transparent;color:#47617b;width:30px;height:30px;cursor:pointer;border-radius:4px;display:grid;place-items:center}.copy:hover{background:#dce6f4;color:var(--blue)}.copy:focus-visible,summary:focus-visible,a:focus-visible{outline:3px solid #b4cdff;outline-offset:3px}.copy svg{width:17px;height:17px}.options{border-block:1px solid var(--line);padding:15px 0;margin:28px 0;color:var(--muted)}summary{color:var(--ink);cursor:pointer;font-weight:550;list-style-position:outside;margin-left:18px}li{margin:10px 0;padding-left:3px}ul{padding-left:21px}.notice{border-left:3px solid #ccd7e8;padding-left:14px;font-size:14px}#empty{padding:20px 0;color:var(--muted)}.sr-only{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap}[hidden]{display:none!important}@media(max-width:850px){.page{padding:28px 20px 50px}header{margin-left:0}h1{font-size:30px}.layout{display:block}aside{position:static;display:flex;gap:20px;overflow:auto;white-space:nowrap;border-bottom:1px solid var(--line);margin-bottom:28px;padding-bottom:10px}h2{font-size:22px}th{display:none}table,tbody,tr,td{display:block;width:100%!important}tr{border-top:1px solid var(--line);padding:20px 0}td{border:0;padding:4px 0!important}td:first-child code{font-size:16px}td:nth-child(2){font-size:16px}td:nth-child(3){margin-top:8px}.example{padding:12px 44px 12px 12px}.copy{top:8px;width:34px;height:34px}table{margin-top:12px}}@media(prefers-reduced-motion:no-preference){html{scroll-behavior:smooth}}
</style></head><body><div class="page"><header><h1>Pecu commands</h1><p>Send a command in your chat with Pecu, or ask in your own words. Reply to a preview with confirm to proceed. YOLO skips confirmation prompts when you explicitly turn it on.</p><div class="search"><label class="sr-only" for="search">Search commands and examples</label><input id="search" type="search" placeholder="Search commands or what you want to do" autocomplete="off"></div><details class="options"><summary>Before using an example</summary><p>All commands use Base mainnet. Replace address placeholders with complete public addresses and position 123 with your own position ID. Amounts are examples. Use the confirmation code the bot gives you; ABC123 is a placeholder.</p><p>Short-command amounts use human token units. For advanced quote, swap, deposit, and AERO-lock commands, include <code>--use-decimals</code> for human amounts. Your wallet is selected automatically.</p></details></header><div class="layout"><aside aria-label="Command sections"><a href="#main">Main commands</a><a href="#reads">Aerodrome reads</a><a href="#transactions">Transaction previews</a><a href="#natural">Ask naturally</a></aside><main>${content}<p id="empty" hidden>No matching commands. Try “balance”, “swap”, or “liquidity”.</p></main></div></div><span class="sr-only" id="copy-status" role="status" aria-live="polite"></span><script>
const copyIcon='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><rect x="8" y="8" width="12" height="13" rx="2"/><path d="M16 8V5a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h3"/></svg>';
for(const code of document.querySelectorAll('td:nth-child(3) code')){if(!/^(?:b)?\\//.test(code.textContent))continue;const box=document.createElement('span');box.className='example';code.replaceWith(box);box.append(code);const button=document.createElement('button');button.className='copy';button.type='button';button.setAttribute('aria-label','Copy '+code.textContent);button.title='Copy command';button.innerHTML=copyIcon;button.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(code.textContent);document.querySelector('#copy-status').textContent='Command copied';button.textContent='✓';setTimeout(()=>{button.innerHTML=copyIcon},1200)}catch{const range=document.createRange();range.selectNodeContents(code);const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);document.querySelector('#copy-status').textContent='Select and copy this command manually';}});box.append(button);}
const search=document.querySelector('#search');const tables=[...document.querySelectorAll('main table')];search.addEventListener('input',()=>{const query=search.value.trim().toLowerCase();let found=false;for(const table of tables){let matches=0;for(const row of table.tBodies[0].rows){row.hidden=!row.textContent.toLowerCase().includes(query);if(!row.hidden)matches++;}table.hidden=matches===0;let heading=table.previousElementSibling;while(heading&&heading.tagName!=='H2'){heading.hidden=Boolean(query)||matches===0;heading=heading.previousElementSibling;}if(heading)heading.hidden=matches===0;found ||= matches>0;}for(const extra of document.querySelectorAll('main details, #natural, #natural ~ p, #natural ~ ul'))extra.hidden=Boolean(query);document.querySelector('#empty').hidden=found;});
</script></body></html>`;
await Bun.write(`${root}/docs/pecu-commands.html`, html);
const actions = [...source.matchAll(/\| `\/aero ([a-z-]+)` \|/g)].map(m=>m[1]);
for(const action of actions) if(!html.includes(`/aero ${action}`))throw new Error(`Missing ${action}`);
console.log(`Built standalone HTML with ${actions.length} Aero actions.`);

{
  "name": "@beegreat/profile",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "deploy": "bun run build && bunx wrangler deploy --config dist/server/wrangler.json",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.7",
    "@beegreat/backend": "workspace:*",
    "astro": "^7.0.9",
    "convex": "^1.42.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
